# دليل نظام إدارة المستخدمين المتقدم

## 🎯 نظرة عامة

تم تطوير نظام إدارة المستخدمين المتقدم الذي يوفر نظام أدوار هرمي متقدم مع صلاحيات محددة لكل مستخدم.

## 👥 أنواع المستخدمين

### 1. مدير النظام (Admin)
- **الصلاحيات الكاملة**: يمكنه رؤية وإدارة جميع المراكز والأفراد والمستخدمين
- **إدارة المستخدمين**: إضافة، تعديل، حذف المستخدمين
- **إدارة الصلاحيات**: تعديل صلاحيات جميع المستخدمين
- **عرض جميع البيانات**: إحصائيات شاملة لجميع المراكز والأفراد

### 2. مشرف مركز (Center Manager)
- **إدارة المراكز المخصصة**: يمكنه رؤية وإدارة المراكز المسموحة له فقط
- **إدارة الأفراد**: إضافة وتعديل الأفراد في المراكز المسموحة
- **إدارة المستخدمين المحدودة**: إضافة مستخدمين للمراكز المسموحة فقط
- **عرض البيانات المحدودة**: إحصائيات للمراكز والأفراد المسموحة فقط

### 3. مستخدم عادي (User)
- **عرض البيانات المحدودة**: يمكنه رؤية المراكز والأفراد المسموحة له فقط
- **لا يمكنه إضافة مستخدمين**: لا يملك صلاحية إدارة المستخدمين
- **صلاحيات محدودة**: حسب الصلاحيات المخصصة له

## 🔐 نظام الصلاحيات المتقدم

### صلاحيات المراكز
- `can_add_centers`: إضافة مراكز جديدة
- `can_edit_centers`: تعديل المراكز الموجودة
- `can_delete_centers`: حذف المراكز

### صلاحيات الأفراد
- `can_add_employees`: إضافة أفراد جدد
- `can_edit_employees`: تعديل بيانات الأفراد
- `can_delete_employees`: حذف الأفراد

### صلاحيات المستخدمين
- `can_add_users`: إضافة مستخدمين جدد
- `can_edit_users`: تعديل بيانات المستخدمين
- `can_delete_users`: حذف المستخدمين

### صلاحيات إضافية
- `can_export`: تصدير البيانات
- `can_track`: تتبع الأفراد
- `can_view_all`: عرض جميع البيانات

## 🏢 ربط المستخدمين بالمراكز

### نظام العلاقات
- كل مستخدم يمكن ربطه بمراكز متعددة
- المستخدم يرى فقط البيانات المتعلقة بالمراكز المسموحة له
- المدير يرى جميع المراكز والأفراد

### جدول العلاقات
```sql
user_centers (
    user_id: UUID,
    center_id: UUID
)
```

## 📊 تصفية البيانات حسب الدور

### للمدير (Admin)
- عرض جميع المراكز والأفراد والمستخدمين
- إحصائيات شاملة
- إمكانية إدارة كاملة

### لمشرف المركز (Center Manager)
- عرض المراكز المسموحة فقط
- عرض الأفراد في المراكز المسموحة
- عرض المستخدمين المرتبطين بمراكزه

### للمستخدم العادي (User)
- عرض المراكز المسموحة فقط
- عرض الأفراد في المراكز المسموحة
- لا يمكنه إدارة المستخدمين

## 🎨 الميزات الجديدة

### 1. واجهة إدارة المستخدمين
- قائمة شاملة بجميع المستخدمين
- بحث وتصفية متقدمة
- تصدير قائمة المستخدمين

### 2. نظام الأدوار المرئي
- ألوان مختلفة لكل دور
- شارات واضحة للأدوار
- تمييز بصري للمستخدمين

### 3. إدارة الصلاحيات التفصيلية
- واجهة سهلة لتعديل الصلاحيات
- حفظ فوري للتغييرات
- عرض الصلاحيات الحالية

### 4. ربط المراكز المتقدم
- اختيار متعدد للمراكز
- عرض المراكز المسموحة لكل مستخدم
- تحديث العلاقات بسهولة

## 🔧 كيفية الاستخدام

### إضافة مستخدم جديد
1. انتقل إلى قسم "إضافة مستخدم جديد"
2. املأ البيانات الأساسية
3. اختر نوع المستخدم (مدير/مشرف مركز/مستخدم)
4. اختر المراكز المسموحة
5. احفظ المستخدم

### تعديل صلاحيات مستخدم
1. انقر على "صلاحيات" بجانب المستخدم
2. حدد الصلاحيات المطلوبة
3. احفظ التغييرات

### إدارة المراكز
1. المدير يمكنه رؤية جميع المراكز
2. مشرف المركز يرى المراكز المسموحة له فقط
3. المستخدم العادي يرى المراكز المسموحة له فقط

## 📱 التصميم المتجاوب

- واجهة متجاوبة لجميع أحجام الشاشات
- تصميم عصري وجذاب
- ألوان متدرجة لكل دور
- أيقونات واضحة ومفهومة

## 🔒 الأمان

- تشفير كلمات المرور
- تسجيل جميع العمليات
- صلاحيات محددة لكل مستخدم
- حماية من الوصول غير المصرح به

## 📈 التقارير والإحصائيات

- إحصائيات مفصلة لكل دور
- تصدير البيانات بصيغ مختلفة
- تتبع العمليات والأنشطة
- تقارير شاملة للمديرين

---

## 🚀 البدء السريع

1. سجل دخولك كمدير
2. انتقل إلى "إدارة المستخدمين"
3. أضف المستخدمين الجدد
4. حدد المراكز المسموحة لكل مستخدم
5. اضبط الصلاحيات حسب الحاجة
6. استمتع بالنظام المتقدم! 🎉
