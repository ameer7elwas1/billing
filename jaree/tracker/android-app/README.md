# تطبيق جاري - تتبع الموظفين للأندرويد

## 📱 نظرة عامة
تطبيق أندرويد متكامل لتتبع دخول وخروج الموظفين من المراكز باستخدام نظام GPS المتقدم. مبني من صفحة `user.html` الموجودة في مجلد `tracker`.

## 🚀 المميزات

### 🔐 نظام المصادقة
- تسجيل دخول آمن برقم الهاتف وكلمة المرور
- حفظ الجلسة لمدة 48 ساعة
- تسجيل دخول وخروج يدوي وتلقائي

### 📍 تتبع الموقع المتقدم
- تتبع GPS بدقة عالية
- حساب المسافة للمركز في الوقت الفعلي
- تسجيل دخول وخروج تلقائي عند دخول/خروج نطاق المركز
- عمل أوفلاين مع مزامنة لاحقة

### 🗺️ خريطة تفاعلية
- عرض موقع الموظف والمركز على الخريطة
- دائرة نطاق المركز
- خط يربط الموظف بالمركز
- تحديث الخريطة في الوقت الفعلي

### 📱 واجهة مستخدم عربية
- تصميم متجاوب للشاشات المختلفة
- واجهة عربية كاملة
- رسائل واضحة للمستخدم
- مؤشرات بصرية للحالة

## 🔗 نفس قاعدة البيانات

يستخدم التطبيق نفس قاعدة البيانات والتفاصيل من `user.html`:

### **Supabase Configuration**
```javascript
const supabaseUrl = 'https://qcbcjyhqgxwuqutzkxrh.supabase.co';
const supabaseKey = 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InFjYmNqeWhxZ3h3dXF1dHpreHJoIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NTEyMDE3NjQsImV4cCI6MjA2Njc3Nzc2NH0.y-hQVa6nzBpCMpXL7XbcWPMGafATvnMez3lAmFvb2zY';
```

### **Mapbox Configuration**
```javascript
const mapboxToken = 'pk.eyJ1IjoiYW1lZXI3ZWx3YXMiLCJhIjoiY21iODAxaTFyMGFsajJqc2J2dGtvY2pmdSJ9.MNY-7y2onlfAnM0PUhNt8g';
```

## 🛠️ التثبيت والتشغيل

### المتطلبات
- Node.js 16+
- Cordova CLI
- Android Studio
- JDK 11+

### خطوات التثبيت

#### 1. تثبيت Cordova
```bash
npm install -g cordova
```

#### 2. إعداد Android SDK
- تثبيت Android Studio
- إعداد متغيرات البيئة:
```bash
export ANDROID_HOME=$HOME/Android/Sdk
export PATH=$PATH:$ANDROID_HOME/tools:$ANDROID_HOME/platform-tools
```

#### 3. بناء التطبيق
```bash
cd android-app
build.bat  # على Windows
```

#### 4. تشغيل التطبيق
```bash
cordova run android
```

## 📁 هيكل المشروع

```
android-app/
├── config.xml              # إعدادات Cordova
├── package.json            # تبعيات المشروع
├── build.bat              # سكريبت البناء (Windows)
├── README.md              # هذا الملف
└── www/
    └── index.html         # التطبيق الرئيسي (مبني من user.html)
```

## 🔧 التخصيص

### تعديل التطبيق
1. افتح `android-app/www/index.html`
2. عدل الكود كما تريد
3. شغل `build.bat` لإعادة البناء

### تعديل الإعدادات
يمكن تعديل الإعدادات مباشرة في ملف `index.html`:
- بيانات Supabase
- مفتاح Mapbox
- إعدادات الموقع
- إعدادات التتبع

## 📱 الأذونات المطلوبة

- `ACCESS_FINE_LOCATION`: تتبع الموقع بدقة عالية
- `ACCESS_COARSE_LOCATION`: تتبع الموقع التقريبي
- `INTERNET`: الاتصال بقاعدة البيانات
- `ACCESS_NETWORK_STATE`: فحص حالة الشبكة

## 🔄 التكامل مع النظام

### البيانات متزامنة
- عندما يسجل موظف دخول من التطبيق → يظهر في لوحة التحكم
- عندما يسجل موظف خروج من التطبيق → يظهر في لوحة التحكم
- جميع السجلات محفوظة في نفس الجداول

### نفس الوظائف
- نفس نظام المصادقة
- نفس منطق التتبع التلقائي
- نفس تسجيل الدخول والخروج اليدوي
- نفس عرض الخريطة والمسافة

## 🚀 النشر

### بناء ملف APK
```bash
cordova build android --release
```

### موقع ملف APK
```
platforms/android/app/build/outputs/apk/release/app-release.apk
```

## 📞 الدعم

للحصول على الدعم:
- راجع ملفات الوثائق في المجلد
- تحقق من سجلات الأخطاء: `adb logcat`
- تأكد من صحة بيانات Supabase و Mapbox

## 📄 الترخيص

MIT License - يمكن استخدامه لأغراض تجارية وشخصية.

---

**ملاحظة**: هذا التطبيق مبني من صفحة `user.html` الموجودة في مجلد `tracker` مع تحسينات خاصة بالأندرويد.
