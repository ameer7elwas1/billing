# 🚀 دليل البدء السريع

## الخطوات الأساسية

### 1️⃣ تقسيم الملف (مرة واحدة)

```powershell
# شغّل السكريبت التلقائي
.\split-file.ps1
```

**أو يدوياً:**
- اتبع `DIVISION_GUIDE.md`

### 2️⃣ إعداد المفاتيح

```bash
# انسخ الملف
copy js\config.example.js js\config.js

# عدّل js/config.js وأضف:
# - SUPABASE_URL
# - SUPABASE_ANON_KEY
```

### 3️⃣ رفع على GitHub

```bash
# تهيئة Git
git init

# إضافة الملفات
git add .

# Commit
git commit -m "Initial commit"

# إضافة المستودع (استبدل USERNAME)
git remote add origin https://github.com/USERNAME/billing-accounts-system.git

# رفع
git push -u origin main
```

**📖 للتفاصيل الكاملة:** اقرأ `GITHUB_GUIDE.md`

## 📁 الملفات المهمة

- `README.md` - نظرة عامة
- `DIVISION_GUIDE.md` - كيفية تقسيم الملف
- `GITHUB_GUIDE.md` - كيفية رفع على GitHub
- `split-file.ps1` - سكريبت تقسيم تلقائي
- `.gitignore` - الملفات التي لا تُرفع

## ⚠️ تذكر

1. **لا ترفع `js/config.js`** (يحتوي على مفاتيح سرية)
2. **احتفظ بنسخة احتياطية** من `billing-new.html`
3. **اختبر التطبيق** بعد التقسيم

## 🆘 مشاكل شائعة

**المشكلة:** الملفات لا تعمل بعد التقسيم
**الحل:** تأكد من:
- جميع الملفات موجودة في المسارات الصحيحة
- الترتيب الصحيح في `index.html`
- فتح Console (F12) للتحقق من الأخطاء

**المشكلة:** خطأ في رفع Git
**الحل:** اقرأ `GITHUB_GUIDE.md` - قسم "حل المشاكل"

