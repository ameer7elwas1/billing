# قسم الحسابات - نظام إدارة الفواتير

## 🎯 نظرة عامة

نظام شامل لإدارة الحسابات والفواتير مع دعم للتحليلات والتقارير المتقدمة.

## 📁 هيكل المشروع بعد التقسيم

```
قسم الحسابات/
├── index.html              # الملف الرئيسي (HTML فقط)
├── styles/
│   └── main.css           # جميع الأنماط (CSS)
├── js/
│   ├── config.js          # الإعدادات والمتغيرات العامة
│   ├── auth.js            # نظام المصادقة والصلاحيات
│   ├── supabase.js        # اتصالات Supabase
│   ├── agents.js          # إدارة الوكلاء
│   ├── reports.js         # التقارير والتحليلات
│   ├── exports.js         # تصدير Excel/PDF
│   ├── utils.js           # الأدوات المساعدة
│   └── main.js            # الدوال الرئيسية والتهيئة
└── README.md              # هذا الملف
```

## ⚡ البدء السريع

### 1. تقسيم الملف الكبير

**الطريقة التلقائية (موصى بها):**
```powershell
# شغّل السكريبت
.\split-file.ps1
```

**الطريقة اليدوية:**
اتبع التعليمات في `DIVISION_GUIDE.md`

### 2. إعداد المفاتيح

```bash
# انسخ ملف المثال
cp js/config.example.js js/config.js

# عدّل js/config.js وأضف المفاتيح الحقيقية
```

### 3. فتح التطبيق

افتح `index.html` في المتصفح

## 🚀 رفع على GitHub

1. أنشئ مستودع جديد على github.com (اسمه: billing-accounts-system)
2. شغّل في PowerShell:
```bash
git init
git add .
git commit -m "Initial commit"
git remote add origin https://github.com/YOUR_USERNAME/billing-accounts-system.git
git push -u origin main
```

**📖 للمزيد:** [`GITHUB_GUIDE.md`](GITHUB_GUIDE.md) أو [`رفع_بسيط.txt`](رفع_بسيط.txt)

### ملخص سريع:

1. **إنشاء مستودع على GitHub**
   - اذهب إلى [github.com](https://github.com)
   - New repository → `billing-accounts-system`

2. **رفع الملفات:**
```bash
git init
git add .
git commit -m "Initial commit"
git remote add origin https://github.com/USERNAME/billing-accounts-system.git
git push -u origin main
```

3. **التحديثات المستقبلية:**
```bash
git add .
git commit -m "وصف التغييرات"
git push
```

## 📝 ملاحظات مهمة

### ⚠️ الملفات الحساسة

**لا ترفع** المفاتيح السرية مباشرة! استخدم ملف `.gitignore`:

```gitignore
# المفاتيح السرية
js/config.js

# ملفات النظام
.DS_Store
Thumbs.db
*.log

# ملفات التطوير
.vscode/
.idea/
```

### 🔐 حماية المفاتيح

1. أنشئ ملف `js/config.example.js`:
```javascript
const SUPABASE_URL = 'YOUR_SUPABASE_URL';
const SUPABASE_ANON_KEY = 'YOUR_SUPABASE_KEY';
```

2. أنشئ ملف `js/config.js` محلياً (لا ترفعه):
```javascript
const SUPABASE_URL = 'https://your-actual-url.supabase.co';
const SUPABASE_ANON_KEY = 'your-actual-key';
```

## 🔄 الترقية من الملف الواحد

إذا كان لديك `billing-new.html` حالياً:

1. **احتفظ بنسخة احتياطية**:
   ```bash
   cp billing-new.html billing-new.html.backup
   ```

2. استخدم الملفات الجديدة المقسمة بدلاً من الملف الواحد

3. تأكد من أن جميع الروابط تعمل:
   - `<link rel="stylesheet" href="styles/main.css">`
   - `<script src="js/config.js"></script>`
   - إلخ...

## 📦 الترتيب الموصى به للملفات

```
js/
├── config.js       (يُحمل أولاً)
├── utils.js        (الأدوات الأساسية)
├── supabase.js     (الاتصالات)
├── auth.js         (المصادقة)
├── agents.js       (الوكلاء)
├── reports.js      (التقارير)
├── exports.js      (التصدير)
└── main.js         (التهيئة - يُحمل آخراً)
```

## 🛠️ التحسينات المطبقة

✅ **الأمان**: استبدال `innerHTML` بـ `createElement` و `textContent`  
✅ **البنية**: تقسيم الملف الكبير إلى ملفات منفصلة  
✅ **التكرار**: دمج الدوال المكررة  
✅ **الأداء**: تحسين استخدام `localStorage`

## 📞 الدعم

إذا واجهت أي مشاكل، تأكد من:
- فتح Developer Console (F12) للتحقق من الأخطاء
- التأكد من أن جميع الملفات موجودة في المسارات الصحيحة
- التحقق من أن المفاتيح في `config.js` صحيحة

