# نظام إدارة الأقساط المدرسية - مجموعة رسول الرحمة

## 📋 نظرة عامة

نظام شامل لإدارة الأقساط المدرسية لجميع مدارس مجموعة رسول الرحمة التعليمية.

## ✨ المميزات

- ✅ إدارة متعددة المدارس
- ✅ نظام دفع متعدد الدفعات
- ✅ حساب الخصومات تلقائياً (إخوة)
- ✅ إشعارات واتساب تلقائية
- ✅ تقارير شاملة (شهرية وسنوية)
- ✅ تصدير Excel و PDF
- ✅ نظام إشعارات ومحادثات بين المدارس
- ✅ لوحة تحكم للمدير العام

## 🚀 التحسينات الجديدة

### 1. قاعدة البيانات المحسّنة (`supabase_schema.sql`)
- ✅ Constraints للتحقق من البيانات
- ✅ Triggers للتحديث التلقائي
- ✅ Functions لحساب الحالات المالية
- ✅ Audit Trail (سجل الأنشطة)
- ✅ Row Level Security (RLS)
- ✅ Indexes محسّنة للأداء
- ✅ Views للاستعلامات المعقدة

### 2. الأمان
- ✅ نقل المفاتيح الحساسة إلى `config.js`
- ✅ تنظيف المدخلات لمنع XSS
- ✅ التحقق من صحة جميع المدخلات
- ✅ تشفير كلمات المرور في قاعدة البيانات
- ✅ استبدال `alert` و `confirm` بنظام Toast احترافي

### 3. الكود
- ✅ تقسيم الكود إلى ملفات منفصلة (`config.js`, `utils.js`)
- ✅ دوال مساعدة قابلة لإعادة الاستخدام
- ✅ معالجة أخطاء محسّنة
- ✅ تحسين الأداء (Debounce, Throttle)

## 📁 هيكل الملفات

```
├── رسول_الرحمة.html      # الملف الرئيسي
├── config.js              # الإعدادات والمفاتيح
├── utils.js               # الدوال المساعدة
├── supabase_schema.sql    # Schema قاعدة البيانات
└── README.md             # هذا الملف
```

## 🔧 التثبيت والإعداد

### 1. قاعدة البيانات

قم بتشغيل `supabase_schema.sql` في Supabase SQL Editor:

```sql
-- سيتم إنشاء جميع الجداول والـ Functions والـ Triggers تلقائياً
```

### 2. الإعدادات

عدّل `config.js` حسب احتياجاتك:

```javascript
const CONFIG = {
    SUPABASE: {
        URL: 'your-supabase-url',
        ANON_KEY: 'your-anon-key'
    },
    // ... باقي الإعدادات
};
```

### 3. الملفات المطلوبة

تأكد من تحميل الملفات بالترتيب التالي:

```html
<script src="config.js"></script>
<script src="utils.js"></script>
<script src="رسول_الرحمة.html"></script>
```

## 🔐 الأمان

### ⚠️ مهم جداً

1. **لا ترفع `config.js` إلى Git** - أضفه إلى `.gitignore`
2. **استخدم متغيرات بيئية** في الإنتاج
3. **غيّر كلمات المرور الافتراضية** فوراً
4. **فعّل RLS Policies** في Supabase

### كلمات المرور الافتراضية

⚠️ **يجب تغييرها فوراً:**

- المدير: `admin` / `master123`
- روضة رسول الرحمة: `rawda` / `rawda123`
- مدرسة رسول الرحمة: `rasoul` / `rasoul123`
- مدرسة نور الرحمة: `noor` / `noor123`
- مدرسة نبي الرحمة: `nabi` / `nabi123`
- ثانوية رسول الرحمة: `thanawiya` / `thanawiya123`

## 📊 قاعدة البيانات

### الجداول الرئيسية

- `schools` - المدارس
- `students` - الطلاب
- `users` - المستخدمين
- `payments` - المدفوعات
- `messages` - الرسائل
- `notifications` - الإشعارات
- `audit_logs` - سجل الأنشطة
- `system_settings` - إعدادات النظام

### Functions المهمة

- `calculate_student_status()` - حساب الحالة المالية للطالب
- `hash_password()` - تشفير كلمة المرور
- `verify_password()` - التحقق من كلمة المرور

## 🎯 الاستخدام

### إضافة طالب جديد

1. املأ جميع الحقول المطلوبة
2. سيتم التحقق من صحة البيانات تلقائياً
3. سيتم حساب الخصم تلقائياً إذا كان هناك إخوة

### تسجيل دفعة

1. اختر الطالب
2. اختر الدفعة
3. أدخل المبلغ والتاريخ
4. سيتم تحديث الحالة تلقائياً

### إرسال إشعارات واتساب

- إشعار دفع: بعد تسجيل أي دفعة
- تذكير: للطلاب المتأخرين
- جماعي: إرسال لجميع المتأخرين

## 🐛 استكشاف الأخطاء

### المشاكل الشائعة

1. **خطأ في الاتصال بقاعدة البيانات**
   - تحقق من `SUPABASE_URL` و `SUPABASE_ANON_KEY` في `config.js`
   - تأكد من تشغيل `supabase_schema.sql`

2. **خطأ في التحقق من المدخلات**
   - تأكد من تحميل `utils.js` قبل الملف الرئيسي

3. **مشاكل في localStorage**
   - امسح cache المتصفح
   - تحقق من مساحة التخزين المتاحة

## 📝 ملاحظات مهمة

1. **الأرقام**: جميع الأرقام تُعرض بالإنجليزية (0-9) حسب تفضيل المستخدم
2. **التواريخ**: تُعرض بالعربية مع دعم صيغ متعددة
3. **التخزين**: البيانات تُحفظ في Supabase مع fallback إلى localStorage

## 🔄 التحديثات القادمة

- [ ] دعم Supabase Auth الكامل
- [ ] إضافة API endpoints
- [ ] تطبيق موبايل
- [ ] نظام نسخ احتياطي تلقائي
- [ ] تقارير متقدمة مع Charts

## 📞 الدعم

للمساعدة أو الإبلاغ عن مشاكل، يرجى التواصل مع فريق التطوير.

---

**الإصدار**: 2.0.0  
**آخر تحديث**: 2024

